// Lista única dos planos ativos -- usada pra gerar as filas de entrada do
// Pipeline dinamicamente (Teste Grátis + uma fila por plano desta lista).
// Um plano novo (ex: "Platina") nasce só adicionando aqui, sem mexer no
// código do Pipeline.
export const PLANOS_ATIVOS = ["Bronze", "Prata", "Ouro"];
